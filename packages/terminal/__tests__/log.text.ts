import { log, success, warning, error } from "..";
import { animate } from './../lib/main';

log("Hello, world");
success("Hello, world");
warning("Hello, world");
error("Hello, world");

animate("Hello, world");
