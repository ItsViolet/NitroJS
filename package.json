{
    "name": "root",
    "private": true,
    "author": "Skylix",
    "devDependencies": {
        "lerna": "^4.0.0"
    },
    "scripts": {
        "prettier": "prettier --write .",
        "release": "lerna run build && lerna publish --exact && lerna run clean",
        "postinstall": "lerna bootstrap"
    },
    "dependencies": {
        "@types/node": "^17.0.8",
        "prettier": "^2.5.1",
        "typescript": "^4.5.4"
    }
}
