{
	"name": "root",
	"private": true,
	"devDependencies": {
		"lerna": "^4.0.0",
		"prettier": "^2.5.1",
		"ts-node": "^10.4.0",
		"typescript": "^4.6.0-dev.20220120"
	},
	"scripts": {
		"build": "lerna exec \"npx tsc --build\"",
		"clean": "lerna exec \"tsc --build --clean\"",
		"format": "prettier --write .",
		"postinstall": "lerna bootstrap"
	}
}
